# BasisState.cc — Python Pseudocode

Implementation of BasisState and GlobalStateIterator. See `BasisState.h.md` for the full pseudocode — the `.cc` file implements exactly those classes and free functions.

```python
# All classes and functions defined in BasisState.h.md are implemented here.
# See BasisState.h.md for the complete pseudocode.
```
