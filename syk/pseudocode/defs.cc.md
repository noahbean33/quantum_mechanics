# defs.cc — Python Pseudocode

Implementation of global definitions. See `defs.h.md` for the full pseudocode — `defs.cc` implements exactly those functions.

```python
# All functions defined in defs.h.md are implemented here.
# See defs.h.md for the complete pseudocode.
```
